services:
  redis:
    image: redis:7-alpine
    container_name: geo_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  app:
    build: .
    container_name: geo_app
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - redis
    restart: unless-stopped